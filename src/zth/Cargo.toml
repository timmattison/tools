[package]
name = "zth"
version = "0.1.0"
edition.workspace = true
description = "Zero the Hero - find files that contain nothing but zero bytes"
license.workspace = true

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
