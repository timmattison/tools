[package]
name = "update-aws-credentials"
version = "0.1.0"
edition = "2021"
authors = ["Tim Mattison"]
description = "Takes AWS credentials from clipboard and updates AWS credentials file"

[dependencies]
arboard = "3.3.0"
aws-config = "1.1.1"
aws-credential-types = "1.1.1"
aws-sdk-sts = "1.9.0"
tokio = { version = "1.32.0", features = ["full"] }
anyhow = "1.0.75"
dirs = "6.0.0"