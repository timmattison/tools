[package]
name = "httpfile"
version = "0.1.0"
edition.workspace = true
description = "Shared static-file serving primitives (content-type lookup, path confinement, and file streaming) for tiny_http-based servers"
license = "MIT"

[dependencies]
tiny_http.workspace = true

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
