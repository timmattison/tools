[package]
name = "portplz-core"
version = "0.1.0"
edition.workspace = true

[dependencies]
sha2.workspace = true
gix.workspace = true
thiserror.workspace = true
libc.workspace = true

[dev-dependencies]
# For `shed_inherited_git_environment`: the fixtures spawn git, and a run from
# inside a git hook inherits an environment aimed at the repository being
# committed to. Which variables those are is knowledge that lives in gitscratch
# rather than being copied here to drift.
gitscratch.workspace = true
tempfile.workspace = true

[lints]
workspace = true
